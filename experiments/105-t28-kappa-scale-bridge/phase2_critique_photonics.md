#### PHOTONICS — verdict: **support-with-changes**

**Steel-man:**
The formula reuse is genuinely disciplined: `beam_geometry(r)` and `sigma_max(κ)=0.5/κ` are verified bit-exact against `design_geometry.py`'s own `R_FAMILY`/`sigma_max_shell` constants, and independently corroborated by the R4 family's `SIGMA_R4_CORRECTED=0.25` at κ=2 — two independently-built geometry families landing on the identical rescaled conductivity is real evidence the optical-depth-preserving convention is physically well-posed, not merely internally self-consistent. Holding the window offset fixed in cells while `r_out` grows correctly reproduces T8's own `z/z_R∝1/r²` near-field-depth-probing logic, and the resulting `x(78):x(156):x(312)=4:2:1` ratio is a genuine geometric consequence, not an assumption smuggled in. Gate P0/P1's zero-cost ground-truth-recovery preconditions are the right discipline before any new-scale reading is trusted, and reusing exp-104's own already-hardened sub-Nyquist quintile/FFT machinery unmodified at each new r avoids reintroducing a confound that cycle spent real FDTD budget resolving.

**Sharpest attack:**
P3's shape-discriminator (Model A sqrt-law 2.00±0.3 vs. Model B linear-law 4.00±0.5) is framed as a fresh, geometrically-forced test, but T8's own already-committed result for the IDENTICAL article (`graded_black_shell`), under the IDENTICAL discriminator definition, already REFUTED both candidate laws on the ambient bench (`experiments/030-.../NOTES.md`: absorber ratio 5.33, outside both bands; PEC ratio −0.635, non-monotonic — "the true r=78→156 drop is sharper... than either simple power law predicts"). The proposal cites T8's methodology at length but never engages this negative prior, presenting two candidate models as though they exhaust the plausible near-field falloff shapes for this exact article. Separately, §5's own z/z_R figures are simply wrong: `77·20/r²` at r=78/156/312 gives 0.2531/0.0633/0.0158 (verified directly), not the stated 0.0253/0.0063/0.0016 — a clean 10× error, absent from the Appendix's own executed script, on the precise quantity anchoring the "1.4–2.4 decades short of witness" claim.

**Parameter change that would flip my verdict:** Before Phase 3 freeze, correct §5's `z_over_zr` arithmetic and add one sentence pre-registering T8's own P-VISION-1b REFUTED-for-both-articles result as prior information bearing on P3 (a miss on this cycle's identical bands would replicate, not surprise) — either fix alone is cheap and zero-FDTD-cost; doing both moves me to full support.

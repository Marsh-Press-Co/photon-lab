# PHASE 2 — RED TEAM FINAL AUDIT · Panel Iteration 82
## exp-105 candidate — "The T8 r=78/156/312 Bridge, Extended to the Coherent Point/Region-Intensity Channel"

*Red Team seat. Receives everything: the Phase-1 proposal and all five blind
Phase-2 critiques (PHOTONICS, MATERIALS, ELECTROMAGNETISM, QUANTUM OPTICS,
VISION SCIENCE), all `support-with-changes`. Per this seat's own charter and
PANEL.md's own spec, every sharpest claim below was independently re-derived
from primitives — source code, `results.json` fields, and LOGBOOK.md's own
verbatim record — not merely trusted from the five critiques. Verified
directly against: `experiments/030-scale-bridge/{NOTES.md,design_geometry.py}`,
`experiments/057-graded-black-shell-flagship-mixed-regime/{NOTES.md,results.json}`,
`experiments/069-t21-block-mini-period-match-power-up/design_geometry.py`,
`experiments/102-coherent-downstream-point-intensity/run.py`,
`experiments/103-t28-gateb-footprint-aperture-match/{run.py,results.json}`,
`experiments/104-t28-subnyquist-standoff-recheck/{run.py,results.json}`,
`lab/materials.py`, `lab/thermo_sidecar.py`, and LOGBOOK.md's RULED OUT
registry (R1–R23) and T8/T13/T14/T28 Live Threads, read in full.*

---

## 0. Independent re-derivation of the three assigned sharpest claims

### 0.1 The `z_over_zr` arithmetic — re-derived from primitives, independently confirmed a **fourth** time, and sharpened: not a single clean 10× error

The proposal's own formula (§2a, stated and used to derive the forced
`x(78):x(156):x(312)=4:2:1` ratio): `z_over_zr(r) = 77 * 20 / r**2`. Executed
directly:

```
r=78:  77*20/78**2  = 0.253123
r=156: 77*20/156**2 = 0.063281
r=312: 77*20/312**2 = 0.015820
```

§5's own Idealizations text states two different, both-wrong, numbers for
this identical quantity in the same sentence:

- **"giving 0.0253/0.0063/0.0016 at r=78/156/312"** — each of these is
  **9.89–10.04×** below the correctly-computed value (ratios: 0.2531/0.0253
  = 10.005, 0.0633/0.0063 = 10.045, 0.0158/0.0016 = 9.888) — a clean,
  uniform ~10× error, consistent with a single decimal-place slip (e.g.
  `D_eff=7.7` substituted for `77`). This is what PHOTONICS, EM, and QUANTUM
  each independently caught, and I reconfirm it exactly.
- **The separately-stated range, `z/z_R∈[0.0026,0.041]`, does NOT match
  either the correct triple `[0.0158,0.2531]` or the 10×-off triple
  `[0.0016,0.0253]`.** I solved for the implied constant: `0.2531/0.041 =
  6.17`, `0.01582/0.0026 = 6.08` — a **third, separately wrong** scaling,
  dividing the correct values by ≈6.1–6.2×, not 10×. EM's critique already
  flagged that the range "doesn't even self-consistently match" the triple;
  I go one step further and confirm it is not scatter around either wrong
  number — it is a *third, independently-computed, also-incorrect* figure.
  **One sentence in this document contains at minimum two mutually
  inconsistent errors on the identical quantity.** A single "fix the 10×
  slip" patch is not sufficient; the whole sentence must be regenerated by
  a printed Appendix statement, not hand-corrected a second time.
- **This figure is absent from both Appendix scripts.** I read both scripts
  in full: the geometry/cost script prints `geom(r)` (N, CX, CY, SRC_X,
  STEPS, R_CORE, sigma_max, tau_shell, margins) and a cost table; the
  thermal script prints the §6 table. Neither ever computes or prints
  `z_over_zr(r)`. The document's own Grounding Note claims "every geometric
  constant below is computed from a stated formula... No number here is a
  fresh FDTD measurement... reproduced in full in this document's own
  Appendix" — a claim this specific sentence violates. This is a direct,
  self-inflicted R4 violation (hand-typed figure, standing house rule) in a
  document that explicitly pledges not to commit one.

**Downstream consequence, independently reconfirmed:** the qualitative
claim built on the wrong numbers — "narrower and shallower than T8's own
original 0.0031–0.049 span" — does not hold under the CORRECT figures
either: T8's own span (exp-030 NOTES.md: "sampled z/z_R∈[0.0031,0.049]")
tops out at 0.049; this cycle's own correctly-computed r=78 value (0.2531)
is **more than 5× past that ceiling**, not narrower/shallower. QUANTUM
already flagged this exact reversal; I independently reconfirm it from the
same primitives.

### 0.2 MATERIALS' Q_ext-anchor claim — re-checked against exp-057's actual `results.json`/NOTES.md language, verbatim

Read `experiments/057-.../NOTES.md` and `results.json` directly.
`results.json::diffraction_inflation_caveat` (line 62), quoted in full:

> "w_on (sigma_ext_cells=240.0073740162445) vs the object's real diameter
> (2*r_out=156 cells, ~1.54x LINEAR excess) is a diffraction-inflated
> optical width — ASSERTED, NOT INDEPENDENTLY BOUNDED (exp-054/NOTES.md).
> ...even that full correction RAISES the margin to ~1655.18x
> (699.27x2.367), still 2+ orders of magnitude clear, in the safe
> direction."

This is **verbatim** what MATERIALS' critique cites — field name, numeric
values (240.0073740162445, ~1.54×), and the exact "ASSERTED, NOT
INDEPENDENTLY BOUNDED" language all reproduce exactly. I also independently
confirmed the proposal's own §6 Appendix script output is genuine, not
fabricated: `dt_ss_full_K` for the r=78 row reproduces
`results.json::dt_ss_full_K = 2.8601275372385233e-05` to the digit, and
`p_abs_w = 1.7409069740390205e-12` likewise reproduces exactly — the
ground-truth-recovery precondition genuinely passes. **MATERIALS' claim
holds on independent re-check: the entire §6 table's r=156/312 rows rest on
an anchor (`σ_ext(78)=240.0`) whose own source document explicitly labels
it "ASSERTED, NOT INDEPENDENTLY BOUNDED," and §6 never restates that
caveat, or the UNOBTANIUM tag, inline** — I confirmed both strings
("UNOBTANIUM", "diffraction") are absent from §6's own text, appearing only
in §5, a different section a future citation of §6's own numeric table
would not necessarily read.

### 0.3 EM's settling-check claim — re-checked by grepping exp-102/103/104's own `run.py`/`NOTES.md` for `STABILITY_TOL` and settling code

`grep -n "STABILITY_TOL"` across all three experiment directories: **zero
hits in exp-102, zero hits in exp-104; one definition in exp-103**
(`run.py:139`, `STABILITY_TOL = 0.20`). Read the settling block
(`run.py:269–298`) in full: it re-runs the empty+article pair at
`STEPS_2X=6400`, computes `k2, i_e2, i_a2 = kappa_region_at(ez_empty_2x,
ez_article_2x, x, CY, H_REGION)` — `H_REGION` is the **wide-box** width
(`kappa_region_1x = trend[x]["kappa_region"]`, itself built from
`kappa_region_at(..., H_REGION)`) — and compares only that against
`STABILITY_TOL`. **`kappa_region_point`/`delta_phi_point` never appear
inside this settling block at all** (confirmed by direct grep — the only
"point" appears three sections later, in the article-vs-empty prediction
scoring, not the settling gate).

`experiments/104-.../run.py:65–73` (its own committed Idealizations text,
read directly): "this cycle runs NO fresh STEPS_2X leg. 'Settling already
established clean' rests on exp-103's own check at x in [352,356] only (a
4-cell span, 5 points) — NOT independently verified across the full
104-cell dense span... relies on the general physical argument... rather
than direct measurement at the farther points." This is exp-104's own
disclosed admission that even its *wide*-channel settling coverage is
partial and argument-based, not measured, at the dense-span points — and
confirms, by its own text, that no settling measurement of any kind exists
for `kappa_region_point`/`delta_phi_point` anywhere in this program.
**EM's claim is confirmed exactly: no settling check on
`kappa_region_point`/`delta_phi_point` exists anywhere in this program's
history, at any r** — and P4, this cycle's own ripple-generalization test,
runs directly and exclusively on that unchecked channel.

---

## 1. Numbered attacks

**1. [inconsistency]** §5's `z_over_zr` sentence contains at least two
independently-wrong figures for the identical quantity (the 10×-low triple
and the separately-wrong ≈6.1–6.2×-low range bracket — §0.1), and the
figure is hand-typed in violation of the document's own stated no-hand-
typed-figure (R4) pledge, since neither Appendix script computes it. A
single-number patch is not sufficient; regenerate the entire sentence from
a printed statement.

**2. [inconsistency]** The document's own Grounding Note ("every geometric
constant below is computed... reproduced in full in this document's own
Appendix") is falsified by attack 1's own existence — the one hand-typed
figure in the document is also the one that is wrong. Before Phase 3
freeze, either every remaining prose-stated numeric claim must be checked
against an Appendix print statement, or the Grounding Note's own scope
must be corrected to name this one exception.

**3. [inconsistency]** Gate P1, as described for the REUSED r=78 leg
("this cycle's own generalized code... using exp-104's own results.json's
captured fields — no new FDTD... exactly matching exp-104's own Gate P1
formula and tolerance"), borrows the evidentiary weight of a check it
cannot structurally replicate. Independently verified from source
(`experiments/104-.../run.py:449–495`): exp-104's own Gate P1 compared a
value computed from a **fresh `Sim.run()` capture** against a stored
reference (`exp-103's results.json::kappa_region_trend`) — a genuine
cross-run engine-reproducibility test. This cycle commits to **0 new FDTD
calls** at r=78. I confirmed `kappa_region_wide = i_region_article /
i_region_empty` (`run.py:187–191`), and that exp-104's own `results.json`
persists those two scalars per x-point — so this cycle's own Gate P1 at
r=78 CAN execute, but only as a division-arithmetic self-consistency check
on numbers exp-104's own script already computed and stored (verifying
exp-104's own division was correct, and that this cycle's file/key-loading
code is not itself buggy) — not an independent regression test of the
FDTD engine or physics the way its named precedent was. The phrase
"exactly matching exp-104's own Gate P1 formula and tolerance" should be
replaced with an explicit statement of what this cycle's own r=78 Gate P1
actually checks (a data-loading/transcription self-consistency guard,
`R6`-lineage but narrower in scope than the precedent it cites), so a
future reader does not credit an r=78 "Gate P1 PASS" with more physics
content than it has. This is structurally the same shape as LOGBOOK's own
Iteration-69/exp-092 precedent — a "zero-cost reuse" claim that assumed
data availability the actual persisted record does not provide — caught
there only by the Director at Phase 3, after all five blind critics and
that cycle's own Phase-2 Red Team audit missed it; naming that precedent
explicitly here is the cheapest way to prevent the analogous miss twice.

**4. [unfalsifiable]** P5's own stated numeric bands ("[100×, 500×] at
r=156... [30×, 250×] at r=312") are explicitly declared non-binding by the
same paragraph: "Not falsified by the classification staying UNDETECTABLE
even outside the illustrative numeric band." I independently re-derived
the underlying physics (`lab/thermo_sidecar.py::mixed_length_scale_regime`,
§0.2): conduction dominates radiative loss by ~3 orders of magnitude
across this entire r-family (1949× at r=78, 487× at r=312, per the
proposal's own Appendix output, independently re-checked against the
function's own `dp_dt = area_m2·(4εσT³ + h_eff)` formula), so `dp/dT ≈
k_air·r_out·dx` — linear in `r_out` — is close to a structural near-
certainty for any `r_out` in [78,312], regardless of whether the disputed
`Q_ext`-invariance assumption holds. That makes the classification-flip
falsification condition close to a null risk this side of a genuine,
multi-order-of-magnitude surprise — the margin-TREND-reversal condition is
the one condition that is genuinely informative (a real reversal would be
a T13/T14-class surprise, this program's own precedent for near-field
non-power-law behavior on this exact bench family), so P5 is not FULLY
unfalsifiable, but its own quoted numeric bands carry zero evidentiary
weight by the paragraph's own explicit disclaimer — a real gap against
PANEL.md's own Phase-1 requirement for "predicted outcomes with falsifiable
bands." Recommend either dropping the illustrative bands from the
falsifiable-prediction framing (keep as descriptive context only) or
committing to a real tolerance around them.

**5. [inconsistency]** PHOTONICS' attack, independently reconfirmed against
`experiments/030-.../NOTES.md` §Results verbatim: "P-VISION-1b (shape
discriminator) — REFUTED for both articles, in different ways" (absorber
ratio 5.33, outside both the 2.00±0.3 and 4.00±0.5 bands; PEC ratio
−0.635, non-monotonic). This cycle's own P3 discriminator uses the
identical forced-geometry bands on the identical article
(`graded_black_shell`) without disclosing this negative prior. A miss
would replicate documented history, not surprise.

**6. [inconsistency]** EM's attack, independently reconfirmed (§0.3): P4
runs on a channel (`kappa_region_point`/`delta_phi_point`) that has never
received a settling check anywhere in this program's history, at any
geometry. The channel this cycle newly commits to (r=156, `STEPS=6400`,
a genuinely new domain) is untested for settling adequacy on precisely the
zero-averaging, single-cell instrument P4's sign-change test depends on.

**7. [inconsistency]** QUANTUM's attack, independently reconfirmed: `z/z_R
∝ 1/r²` (an algebraic consequence of the proposal's own fixed-`D_eff`,
fixed-`λ_cells` construction, confirmed in §2a) forces the Fresnel number
`N_F=r²/(λ·D_eff)` to grow exactly 16× from r=78 to r=312, while
`DENSE_PITCH` stays fixed at 2 cells absolute at every r (confirmed:
`experiments/104-.../run.py:135`, `range(352, 457, 2)`, reused unmodified
per §2a's own table). No computed prediction of where a genuine ripple's
period should land at the new scales exists in the proposal. A FALSIFIED
P4 at r=312 could be a second-generation aliasing null rather than a
genuine one.

**8. [inconsistency]** MATERIALS' attack, independently reconfirmed
verbatim against `experiments/057-.../results.json` (§0.2): the §6
thermal table's entire numeric content depends on an anchor its own
source document labels "ASSERTED, NOT INDEPENDENTLY BOUNDED," and §6
never restates that caveat or the UNOBTANIUM tag inline (confirmed absent
from §6's own text by direct read).

**9. [inconsistency]** VISION's attack, independently reconfirmed against
`experiments/104-.../run.py:148–150`: the `DISCLAIMER` string ("Raw
physical intensity/phase ratios only... not a claim about human
visibility") never mentions NETD or the instrument-threshold distinction
§6's own newly-live P5 classification needs. This is not a generic
concern — this exact T28 sub-thread has independently fired Checkpoint
criterion 4 at Iteration 65 specifically over a disclaimer failing to
propagate on the identical NETD/thermal-sidecar channel (LOGBOOK R16/R21,
and the Iteration-65 CHECKPOINT entry's own fourth-instance ruling). A
disclaimer scoped correctly for exp-102/103/104 (thermal N/A there) does
not automatically cover exp-105 (thermal live for the first time).

---

## 2. Flip conditions — ruling

**ADOPT all five, in full, none overridden.** Independently re-derived
every load-bearing claim behind each (§0, §1) and found all five hold
exactly as stated; none rests on an unverified assumption, a look-elsewhere
artifact, or a misapplied precedent. Each is cheap (a sentence, a
2-call FDTD leg, a desk-only period-band computation) and none requires
abandoning the cycle's own scope or budget. Specifically:

- **PHOTONICS** (correct §5 arithmetic + pre-register P-VISION-1b as
  prior information for P3) — ADOPT, folded into attacks 1/2/5 above.
- **MATERIALS** (one sentence in §6 restating the Q_ext-invariance/
  diffraction-inflation caveat inline) — ADOPT, attack 8.
- **ELECTROMAGNETISM** (mandatory doubled-STEPS settling check on
  `kappa_region_point`/`delta_phi_point` at r=156, gating P4) — ADOPT,
  attack 6. This is the single most consequential of the five: without
  it, P4's own r=156 verdict is not distinguishable from a settling
  artifact, and P4 is this cycle's only genuinely new physics question
  (P0–P3 are reproducibility/functional-form checks; P4 asks whether a
  real phenomenon generalizes).
- **QUANTUM OPTICS** (compute a Fresnel-forced predicted-ripple-period
  band before trusting P4's verdict at the new scales) — ADOPT, attack 7.
- **VISION SCIENCE** (extend the `DISCLAIMER` string to name NETD
  explicitly, asserted present wherever P5's verdict is reported) —
  ADOPT, attack 9, elevated to mandatory given this sub-thread's own
  four-instance disclaimer-erosion history (R16/R21/R23 lineage).

No flip condition is overridden. None asked for something expensive,
speculative, or outside this cycle's own disclosed scope; all five
target genuine, independently-reproducible gaps.

## 3. New defects — none of the five caught

Two, both above: **attack 1's sharpened finding** (the range bracket is a
*third*, independently-wrong figure, not scatter around the already-known
10× error — EM flagged the mismatch but not that it resolves to a distinct
≈6.1–6.2× miscalculation), and **attack 3** (Gate P1's actual, narrower
epistemic content for the reused r=78 leg, and its structural kinship to
the Iteration-69/exp-092 "unexecutable zero-cost reuse" precedent). Attack
4 (P5's self-disclaimed numeric bands) is a genuinely new observation, not
raised by any of the five, though it is a moderate rather than severe gap
given the underlying physics is not fully tautological.

## 4. Overall verdict

**PROCEED-WITH-MANDATORY-FIXES.**

The core instrumentation methodology is sound and, on independent
re-derivation, genuinely disciplined: the r-family formula chain
reproduces T8's own established constants bit-exact at κ=1 (Gate P0, a
real check, independently reconfirmed); the `sigma_max(κ)=0.5/κ` rescale
independently cross-validates against the R4 family's own
independently-built `SIGMA_R4_CORRECTED=0.25` at κ=2 (confirmed exact
against `experiments/069-.../design_geometry.py`); the r=312 cost-gating
rule is a genuine, pre-committed response to T8's own documented 8×
timing miss; T1 is correctly N/A and constraint-3/4 scoring is correctly,
repeatedly, and explicitly declined — **constraint 3 is not quietly
dropped anywhere in this document; it is disclaimed by name in three
separate sections (§3, §5, §6's own P5 framing) and no scored perceptual
claim is smuggled in.** None of the nine numbered attacks above rises to
`reject` — every one is a cheap, mechanical, zero-to-low-marginal-cost fix
(a corrected sentence, a 2-call FDTD leg, a desk-only formula, a disclaimer
string extension) that does not require abandoning the r=156/312 build or
its own disclosed budget.

## 5. Must land before predictions frozen — priority order

1. **Regenerate §5's `z_over_zr` sentence from a printed Appendix
   statement** (attacks 1, 2) — the highest-priority item precisely
   because it is the one place this document's own no-hand-typed-figure
   discipline broke, and it broke twice in one sentence. Also correct the
   downstream "narrower and shallower than T8's span" / "1.4–2.4 decades
   short" language once the correct figures are in hand (§0.1 shows the
   correct r=78 value is 5× PAST T8's own span ceiling, not narrower).
2. **Add EM's doubled-STEPS settling check on `kappa_region_point`/
   `delta_phi_point` at r=156** (attack 6) — gates P4, this cycle's only
   genuinely new physics question; 2 extra FDTD calls, zero scope
   expansion.
3. **Add QUANTUM's Fresnel-forced predicted-ripple-period band** at each
   committed r, confirming `DENSE_PITCH` clears it by the sub-Nyquist
   margin exp-104 established (or scale `DENSE_PITCH(r)∝1/κ`) before P4's
   FALSIFIED verdict is trusted at r=156/312 (attack 7) — zero FDTD cost.
4. **Extend the `DISCLAIMER` string to name NETD explicitly**, asserted
   present wherever P5's verdict is reported (attack 9) — mandatory given
   this sub-thread's own four-strong disclaimer-erosion recurrence
   history; the cheapest fix on this list.
5. **Add MATERIALS' one-sentence Q_ext-invariance/diffraction-inflation
   caveat inline in §6**, immediately before the numeric table (attack 8).
6. **Pre-register T8's own P-VISION-1b REFUTED-for-both-articles result**
   as prior information bearing on P3 (attack 5, folded from PHOTONICS).
7. **Clarify Gate P1's actual scope for the reused r=78 leg** — state
   explicitly that, given 0 new FDTD calls, it is a data-loading/
   transcription self-consistency check against exp-104's own already-
   computed numbers, not an independent cross-run physics reproducibility
   test (attack 3) — lower priority than 1–6 since it does not change any
   number, only what evidentiary weight a reader should assign to a PASS.
8. **(Recommended, not blocking)** Either drop P5's illustrative numeric
   bands from the falsifiable-prediction framing or commit to a real
   tolerance around them (attack 4).

Items 1–6 are genuinely mandatory (each traces to an independently
re-derived, load-bearing defect); items 7–8 are lower-priority precision
fixes that improve the document's own honesty about what its checks
establish without changing any committed number or verdict.

## 6. Checkpoint criterion 4 — explicit ruling

**Not close to firing this cycle, and I state this explicitly rather than
leaving it implicit.** This is instrumentation/diagnostic work (T1: N/A,
as both T8's founding cycle and exp-102 were), constraint-3/4 scoring is
correctly and repeatedly declined rather than quietly dropped, and no
unfalsifiable claim survives unqualified into this document's own
predictions (P5's soft bands are self-disclaimed as non-binding, not
silently presented as decisive — attack 4 asks for tightening, not for
correcting a hidden overclaim). Every defect named above — including the
sharpest one (the doubly-wrong `z_over_zr` sentence, a genuine R4-class
hand-typed-figure violation) — was caught **at Phase 2, blind, before any
Phase 3 freeze**, independently by three of five critiques (PHOTONICS, EM,
QUANTUM) and reconfirmed here a fourth time, exactly matching this
program's own dozens of established non-firing precedents (R4 through R23,
every one of which discharges when "caught blind, same cycle, before
Phase 3 froze anything"). R20's own three-R4-defect Checkpoint-4-grade
recurrence clause requires defects to have **survived a document's own
Phase-3 prediction-freeze into its Result/Learned sections** — this
document has not yet reached Phase 3, so R20 cannot apply here by its own
text. The one item worth naming forward, not as a firing risk but as a
standing caution for the Director's own Phase 3/4/5 handling: given this
exact T28 sub-thread's documented four-instance disclaimer-erosion
history (R16/Iteration-53, R21/Iterations 76–77, and the Iteration-65
CHECKPOINT firing specifically on this NETD/thermal-sidecar channel), item
4 above (the `DISCLAIMER` extension) should be treated as load-bearing
through Phase 4/5, not merely a Phase-3 wording fix — if P5's own
classification and margin-decline finding are not carried inline into
whatever Result/Learned prose this cycle eventually produces (R21's own
standing requirement), that would be a fifth instance on this exact
lineage and a much sharper candidate for firing than anything in this
document today.

#### VISION SCIENCE — verdict: **support-with-changes**

**Steel-man:** This cycle gets the discipline right where my seat's charter
bites hardest. Perceptual scoring is explicitly ruled out, not smuggled in
(§3, §5), and the two live instruments — κ (a raw phasor-derived intensity
ratio) and NETD (an instrument-threshold classification, orthogonal to the
human eye's own ~10 µm blindness to thermal re-emission, T5) — are kept
apart from any C_thr(L)/Weber-contrast comparison, avoiding the exact
R9-class unit-conflation this sub-thread was burned by once already
(T16's `amp_ratio`-vs-`C_thr` 24×/0.12× episode). Gate P0/P1 apply this
program's own R6/R15 ground-truth-recovery discipline proactively, and I
verified `sigma_max=0.25`/`R_CORE=60` at κ=2 against
`experiments/069-.../design_geometry.py` and `p_abs_w`/`699.27×` against
`experiments/057-.../results.json` — both reproduce exactly. The R23 plan
is correctly scoped to Phase 3, not over-claimed as already implemented.

**Sharpest attack:** §6 activates the THERMO sidecar for the first time on
this channel, producing a live, *scored* NETD classification (P5) — but
§5's R23 plan commits only to matching exp-104's `DISCLAIMER` string,
which never mentions NETD (thermal was N/A there). Reused byte-for-byte
at Phase 3, as R23 demands, NETD sits undisclaimed — R23's own founding
scope gap ("covers only ONE of at least three disclaimers... the
THERMODYNAMICS thermal-sidecar-N/A sentence... remain[s] manual prose")
recurring on that exact channel, now *live*, not dormant. Separately:
§4's P2 language — "the object DARKENS," "shadow continuing to deepen" —
is the identical visual-vocabulary leak my own seat flagged at exp-103's
Phase 2 ("shadow," "fills in," "floor"), whose fix that cycle adopted as
§5's own disclaimer. R23 checks substring transcription, not content
adequacy (my exp-104 Phase-5 finding) — the same gap, one document later.
Minor: §3 claims the disclaimer is "restated in §4"; it is only §5.

**Parameter change that would flip my verdict:** Extend the single
`DISCLAIMER` string Phase 3 writes (per R23) to name the NETD/thermal
channel explicitly — e.g. "...; the NETD classification is an
instrument-detectability threshold, not a claim about human visibility"
— and assert its presence in whatever text block reports P5's verdict,
not only in `PREDICTIONS_TEXT`/`RESULT_TEXT`'s existing κ-scoped form.
Absent that one extension, this seat's verdict stays
support-with-changes, not full support.

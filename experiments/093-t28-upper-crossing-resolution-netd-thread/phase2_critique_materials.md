# PHASE 2 — CRITIQUE · MATERIALS & METAMATERIALS (fresh context, blind) · exp-093 · Panel Iteration 70

*Fresh sub-agent, no memory of any prior cycle, no access to any other
seat's current-cycle critique. Read in full: `PANEL.md`; `LOGBOOK.md`
(RULED OUT R1–R15 in full — with particular depth on R13/R14/R15's own
founding texts and this seat's own founding role in R15 — plus ESTABLISHED
and the complete T28 live thread, Iteration 46/exp-069 through Iteration
69/exp-092); `experiments/093-.../phase1_proposal.md` in full; exp-092's
own `NOTES.md`, `results.json`, `phase5_review_materials.md` (this seat's
own prior-cycle review) and `phase5_redteam_audit.md` (§7–§8, the
reconciled queue this proposal claims to execute); exp-091's own
`phase5_review_materials.md` (this seat's own R15-founding review).*

## Steel-man (≤150 words)

This proposal is the most R15-disciplined instrument cycle T28 has
produced. Item 3 correctly executes this seat's own top exp-092
recommendation — a sigma_max check localized to the actual near-null,
not extrapolated from the three broader census angles Rank 3 tested — and
sequences it *before* item 1 spends any of its 24 calls on that article,
the identical Rank-3-before-Rank-1 discipline exp-092's Red Team had to
force, applied here correctly without being told twice. Item 2's `n=8`
table is genuinely zero-mixed-resolution (every row `cpl=30`, unlike
exp-090's `cpl=20`-only zone R15 was founded to distrust), and both the
AUC/direction reversal and its own "not yet a resolved reversal" caveat
are stated with real numbers, not merely asserted. Item 4 finally
discharges the twice-deferred Yee-dispersion citation under R8. The
`sigma_max=1/3` correction is correctly treated throughout as pure
numerical renormalization — no physical-material claim is smuggled in.

## Sharpest attack (≤150 words)

Item 2's "`n=8` `cpl=30`-only" zone does not complete R15's founding
mandate — it reinstantiates R15's own failure shape one grid level up. My
own founding review (exp-091 §5) named two discharge paths: (a) all seven
original points R3-verified, or (b) a genuinely resolution-aware regressor
replacing FLOOR-margin. Item 2 does neither: three of exp-090's seven
points still lack any `cpl=30` measurement (disclosed, but that leaves
condition (a) unmet), and the zone construction reuses the identical
FLOOR-margin/Firth machinery, never itself validated stable across
resolutions since `cpl=40` is explicitly declined this cycle. Nothing
establishes `cpl=30` is converged rather than merely "a different single
resolution than `cpl=20`." §13's claim that items 1/2 are "the direct
completion of R15's own founding mandate" overstates what one new
fixed-resolution snapshot, however cleanly built, can establish — the
AUC-reversal is honestly hedged in prose, but §13's own header still
claims completion the data doesn't support.

## On the sigma_max branch-gating logic (item 3 gating item 1)

No realizability blind spot to flag. `sigma_max ∈ {0.5, 1/3}` is not a
proposed material conductivity — it is a pure grid-rescaling correction
holding `τ_center = 2·σ·r_out(cells)` invariant under `cpl` refinement,
independently traced to the source formula in this seat's own exp-092
review (§4) and confirmed there to coincide numerically, but not
physically, with `REALIZABILITY_MEMO.md` Entry 2's unrelated real-object-
scaling formula. Item 3's branch gate tests numerical-model self-
consistency, correctly disclaimed (Idealization 11) as not revalidating
comparability if it fires REFUTE/NEITHER. One process gap, non-blocking:
this cycle does not carry forward the one-sentence disambiguating note
this seat's own exp-092 review recommended for future citations of
`sigma_max=1/3` — worth adding in the same-shift docket, not a reason to
withhold support.

## Verdict: **support-with-changes**

## Single change that would flip this to full support

Reword §13's R15 bullet (and the corresponding future NOTES.md framing)
from "item 1/item 2 together are the direct completion of R15's own
founding mandate" to something that names both still-open discharge
conditions explicitly — e.g. "a further, `cpl=30`-verified step toward
R15's founding mandate; not yet its completion, since three of exp-090's
seven original points remain un-R3-verified (Tier 3) and no second
`cpl=30`-adjacent resolution point exists to confirm `cpl=30` itself,
rather than `cpl=20`, is the converged one." This is a scope-claim fix,
not a redesign — it costs zero FDTD and does not touch any scored
prediction — but it is load-bearing: a future cycle citing exp-093 as
"R15 closed" would be citing a claim this cycle's own data does not
support.

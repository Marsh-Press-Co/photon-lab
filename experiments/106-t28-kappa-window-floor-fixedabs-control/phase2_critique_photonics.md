#### PHOTONICS — verdict: **support-with-changes**

**Steel-man:**
Item 4's geometric parity is genuinely clean: `R_COAT=r` is held identical between families at each r (only `R_CORE`/`sigma_max` differ, §2b), and `tau_shell=sigma_max·thickness=24.0` is held exactly fixed in BOTH families at every r (0.5×48 fixed-abs, 0.125×192 self-similar at r=312) — so any diffraction geometry difference (z/z_R, window standoff, D_EFF=77 cells fixed) is identical between arms by construction, isolating exactly one variable: whether the coating's electrical thickness (2.4λ fixed vs. 2.4λ→9.6λ growing) matters given equal net optical depth. That is the right control for the confound Red Team named. Reusing r=78 (0 new calls, both families numerically identical) and gating item 4's own scoring on p3-trusted-equivalent Nyquist/settling status (§4, "scored only if...") correctly refuses to let a MARGINAL r=312 read masquerade as decisive.

**Sharpest attack:**
Because `tau_shell=24` AND `R_COAT` are literally identical between families at every r, the pure-diffraction hypothesis makes a much stronger, more falsifiable prediction than the proposal scores: near-*identical absolute* `kappa_window_fixedabs(r)` vs. `kappa_window_selfsim(r)` at r=156 (8.867e-4) and r=312 (4.793e-6) — since the outer boundary that generates the near-field diffractive tail is unchanged, and a near-opaque shell (τ=24, transmission ~exp(-24)) should render the *inner* PEC-core-size difference (60/108 at r=156; 120/264 at r=312) nearly unobservable at the window. Instead §4 only scores `shape_ratio` (a 3-point ratio-of-differences, one point — r=78 — literally shared data between arms), a strictly weaker, more degenerate statistic that both families' fresh captures will already produce for free but that the proposal never states as a bounded, falsifiable band. Separately, the ≤8.0 CONFIRM band (n≤3.0, `log₂`) still sits well above NOTES.md's own cited theory-motivated edge-diffraction exponent (n≈1–2) — so neither pre-registered outcome actually reconciles the finding with known diffraction physics; the control can only rule *between* two program-internal hypotheses, not connect either to a coherent optical mechanism.

**Parameter change that would flip my verdict:** Add a pre-registered, falsifiable band on the raw per-r cross-family ratio `kappa_window_fixedabs(r)/kappa_window_selfsim(r)` (e.g., within a factor of ~2 → geometric-window dominance strongly corroborated; outside it → thickness effect real at the absolute-magnitude level, not just in shape) at r=156 and r=312 — zero marginal FDTD cost since both scalars are already captured; this closes the gap between what the data can show and what §4 commits to scoring.

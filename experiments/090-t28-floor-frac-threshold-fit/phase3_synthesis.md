# PHASE 3 — SYNTHESIS · Panel Iteration 67 · exp-090 · Director

## Disposition of Phase 2

**All five blind critiques and all nine of Red Team's mandatory-fix docket items are ADOPTED IN FULL. Zero overridden.** Red Team's own audit upheld every one of the five blind critiques on independent re-derivation (MATERIALS, ELECTROMAGNETISM, THERMODYNAMICS, QUANTUM OPTICS, VISION SCIENCE), added three of its own (RT-1, RT-2, RT-3), and returned PROCEED-WITH-MANDATORY-FIXES. I re-verified every load-bearing number myself, independently, before writing this synthesis (see "Director's own verification" below) and find no basis to override any item — the docket is uniformly cheap (prose disclosures plus one small, well-specified computation), and every fix strengthens the deliverable rather than narrowing its scope.

Disposition, one line each:

1. **Dual-section carried-idealizations banner (VISION, upheld by Red Team)** — ADOPTED. Applied in NOTES.md below at both Predictions and Result sections, per the Iteration-65 CHECKPOINT's mandatory (non-discretionary) requirement. Red Team's own explicit shape-ruling (§3 of its audit) — that this is the *same recurring omission shape* as the lineage that fired Checkpoint 4 at Iteration 65, but does **not** itself fire this cycle because it was caught blind, by a Phase-2 critique, before Phase 3 froze anything, matching this program's standing discharge test — is accepted as Director's own ruling too, for the same reasons Red Team gives. I additionally accept Red Team's governance observation at face value: this is the second consecutive T28 Phase-1 lead draft to miss this exact just-escalated requirement, and I am naming a mechanical-lint follow-up in NOTES.md's Next section rather than treating two clean catches as evidence the gap is closed.
2. **R3 spatial-resolution disclosure (MATERIALS)** — ADOPTED. Disclosed as Idealization 9 below, explicitly naming that the already-queued exp-089 Tier-1 item 3 (cpl resolution check) will discharge this gap for this fit's own inputs too, not just for T28's mechanism question.
3. **Forward-sampling-bias disclosure (THERMODYNAMICS)** — ADOPTED. Disclosed as Idealization 10 below: the caution zone is a classification-trust label only and must not be read as a signal to deprioritize sampling in the CAUTION region — if anything R14's own established `σ_ext(θ)`-differential concentration there argues for oversampling.
4. **Reword P2's evidentiary claim (ELECTROMAGNETISM)** — ADOPTED.
5. **Reword P5's falsifiability claim (QUANTUM OPTICS)** — ADOPTED.
6. **Reclassify P2 and P5 from falsifiable predictions to diagnostic sanity checks (Red Team's compounding finding on items 4+5)** — ADOPTED. This is the single most substantive change from Phase 1: P1, P3, and P4 remain this cycle's load-bearing, falsifiable deliverables; P2 (the exact permutation test) and P5 (the LOO jackknife) are retained and still computed — they remain useful as pipeline-correctness sanity checks and as concrete illustrations of the zone's point-sensitivity — but are no longer scored as independent evidence for or against the margin–outcome relationship, since both are, on independent re-derivation by two seats and this synthesis, deductive consequences of facts (P1's perfect separation, R13/exp-089's own prior denominator-driven decomposition) already on the record before this cycle ran.
7. **37.2°'s own pre-existing resolved-gate fragility (Red Team RT-1)** — ADOPTED. Disclosed explicitly next to the zone's upper edge and `m₅₀` in the Result section design below (Q7, new this cycle), and the "drop 37.2°" LOO scenario is reframed as the operationally primary sensitivity reading, not an undifferentiated one-of-seven case.
8. **Compute, not merely argue, the distance-to-crossing regressor comparison (Red Team RT-3)** — ADOPTED, and I did the computation myself before this synthesis (below), rather than deferring an uncomputed claim into Phase 4 unverified. This surfaces a genuinely new, non-obvious quantitative finding (below) that changes §5's own framing from "no information gain" to "gain, but margin is measurably more robust" — a materially better-supported statement, and I am folding this into Phase 4's `run.py` as its own gated, pre-registered prediction (Q8) rather than leaving it as Director-only scratch work, so it is independently reproducible exactly like every other numbered prediction.
9. **Curated/crossing-proximity-enriched sample disclosure (Red Team RT-2)** — ADOPTED. Disclosed as Idealization 11 below.

## Director's own independent verification (before writing any frozen prediction)

Before freezing anything, I independently re-implemented, from scratch, every numbered computation in the proposal plus Red Team's item 8, in a throwaway script, and confirm:

- **Table 1 (7-point margin/ratio_k table): bit-exact** to the proposal's and both audits' own figures. `AUC(margin)=1.0`, zone `[1.4764, 2.1709]`.
- **Exact permutation test**: `p = 1/21 = 0.047619047619047616`, `count_ge=1` of `total=21`, bit-exact.
- **Naive (unpenalized) MLE**: diverges as predicted — 2000 Newton–Raphson steps drive `β→(65.0, −256.8)` and climbing, confirming P1's diagnosed hazard directly (not merely asserted).
- **Firth's penalized fit**: converges in **20** iterations (proposal predicted ≤200; two Phase-2 seats independently got 19 — the 1-iteration difference is convergence-tolerance/implementation-detail noise around `‖Δβ‖<1e-10`, not a discrepancy in the answer) to `β=(1.78058857, −5.6315192)`, `m₅₀=2.0710120823026736` — matches the proposal's own informal `≈2.07` and both critiques' bit-exact figures.
- **LOO jackknife**: all 7 subsets preserve `AUC=1.0` exactly as QUANTUM's algebraic argument requires; zone edges move only when the current argmin (41.4°) or argmax (37.2°) is the point dropped, exactly as predicted — dropping 41.4° moves the lower edge to 1.3095 (40.2° becomes the new max-X); dropping 37.2° moves the upper edge to 3.8793 (36.0° becomes the new min-C).
- **Out-of-sample 38.6°**: `margin=0.3865`, `P(Y=1)=0.9838` under the fitted Firth model, below the zone's lower edge — both readings agree with R13's own existing exclusion.
- **NEW — Red Team item 8, the distance-to-nearest-known-crossing sensitivity model**: I independently located all four zero-crossings of `delta_scene(θ)` in exp-083's own committed 31-point window by linear interpolation between adjacent sign changes: **37.1272°, 38.5902°, 40.2654°, 41.4609°** (the first and last two match exp-089's own `≈37.13°`/`40.265°`/`41.461°` citations to the sub-0.01° digit; 38.5902° matches the R13-founding node). Nearest-crossing distance at the 7 resolved angles: `36.0°→1.1272°, 37.2°→0.0728°, 38.4°→0.1902°, 38.8°→0.2098°, 40.2°→0.0654°, 41.4°→0.0609°, 41.8°→0.3391°`. **This regressor ALSO achieves perfect separation** (`AUC(distance)=1.0`, zone `[0.0654°, 0.0728°]`) — so §5's collinearity argument correctly predicts *that* distance separates too, but the **relative gap is far thinner**: the distance-zone's gap ratio (`upper/lower = 0.0728/0.0654 = 1.1121`) is roughly a third of the margin-zone's own gap ratio (`2.1709/1.4764 = 1.4704`). Both regressors agree on every classification at this sample size, but **margin is the empirically more robust choice by a real, now-measured margin of safety, not merely a theoretically equivalent one** — a materially stronger and more precise statement than §5's original argued-not-computed claim, and I am carrying this forward as Q8, a genuine (if low-drama) new falsifiable prediction for Phase 4 to reproduce.

Nothing in this list surprised me relative to what the proposal and all six Phase-2 documents already claimed — this is a correctly-specified, thoroughly cross-checked method. The synthesis work below is disclosure and scope discipline, not a substantive redesign.

## Frozen experiment spec (Phase 4 `run.py` must implement, bit-exact)

1. Recompute Table 1 from each source experiment's own `results.json::frac_contrast` (not hand-copied), against `FLOOR=FLOOR_FRAC(0.10)×RMS[frac_contrast]=1.91744×10⁻⁴` (unchanged since exp-088).
2. **Q1 (diagnostic, not scored — precondition).** Report `AUC(margin)` and confirm `=1.0` with no ties; report the naive unpenalized MLE's divergence explicitly (a demonstration, not a scored prediction).
3. **Q2 (diagnostic sanity check, reclassified from falsifiable — item 4+6).** The exact permutation test, `p=1/21`, reported with the corrected framing: confirms internal arithmetic/pipeline correctness, not independent evidence the margin–outcome link is real (that link is already established by exp-089's own decomposition).
4. **Q3 (PRIMARY, falsifiable).** The non-parametric caution zone, `zone=[max{margin:Y=1}, min{margin:Y=0}]`.
5. **Q4 (PRIMARY, falsifiable, genuinely contingent).** Firth's penalized logistic fit of `Y` on `log₁₀(margin)`: converged finite `(β₀,β₁)`, `m₅₀=10^(−β₀/β₁)`, predicted to converge and to land inside the Q3 zone.
6. **Q5 (diagnostic sanity check, reclassified from falsifiable — item 5+6).** The LOO jackknife, reported as a deterministic illustration of point-sensitivity (guaranteed by Q1's own tie-free separation, per QUANTUM's and this synthesis's independent algebraic confirmation), not a stress test with a live falsification condition.
7. **Q6 (context, unscored, unchanged from exp-089's own convention).** Out-of-sample scoring of 38.6° against the fitted model and the zone.
8. **Q7 (NEW — disclosure gate, Red Team RT-1).** Recompute 37.2°'s own `resolved`-gate noise-floor margin from raw `box_dev`/`p_abs_w` primitives (a *different* quantity from Q3's `frac_contrast`-floor margin) and report it explicitly beside the zone's upper edge and `m₅₀`; report the "drop 37.2°" LOO scenario as the named, operationally primary sensitivity case. Predicted: reproduces exp-089's own cited `1.046×` (this synthesis's own desk check landed at `1.0455×` — see below for why the two aren't in conflict).
9. **Q8 (NEW — PRIMARY, falsifiable, Red Team RT-3, computed above, re-run inside `run.py` for independent reproducibility rather than left as Director scratch work).** The distance-to-nearest-known-crossing single-regressor sensitivity model: locate all zero-crossings of `delta_scene(θ)` in exp-083's own 31-point window; compute nearest-crossing distance at the 7 resolved angles; compute `AUC(distance)` and the distance-zone's gap ratio; compare against margin's own gap ratio. Predicted: `AUC(distance)=1.0` (perfect separation, matching margin), distance-zone `≈[0.065°, 0.073°]`, gap ratio `≈1.11`, materially thinner than margin's own `≈1.47` — i.e., **both regressors separate; margin is empirically the more robust choice**, not merely a theoretically-argued equivalent one.

## Q7's own "1.046× vs 1.0455×" note (record hygiene, R4/R9 discipline, resolved before it could become a discrepancy)

exp-089's own NOTES.md prints 37.2°'s `resolved`-gate noise-floor margin as "1.046×" (3 decimal digits, EM's own Phase-5-cited figure). My own from-scratch recomputation from raw `box_dev_max`/`p_abs_w` primitives lands at `1.0455×`. These are the same quantity to the precision each was printed at (`1.0455` rounds to `1.046` at 3 s.f. only if read as 3 significant figures rounding conventionally — `1.0455→1.046` under round-half-up at the 4th digit, which is what happened) — **not a discrepancy, a rounding-display difference**, and Red Team's own audit independently reproduced the same `1.0455×` figure I did. Stated explicitly here so no future cycle re-litigates this as a live inconsistency.

## Idealizations carried into NOTES.md (numbered there 1–11, superseding exp-090's own draft numbering)

All of exp-089's Idealizations 9/10/16 (NETD-not-human-eye, constraints 1-4 not tested, FLOOR/RMS material-and-wavelength specificity) carry forward unchanged and now correctly appear at BOTH the Predictions and Result sections of NOTES.md (fix item 1). New idealizations added this cycle per the docket: R3 spatial-resolution gap (item 2), forward-sampling-bias caveat (item 3), n=7 sample-curation caveat (item 9), plus the pre-existing small-sample/scope caveats from the Phase-1 proposal (§6 items 1,2,4,6,8 there) carried through unchanged.

## What "PROCEED" looks like operationally

Phase 4 is a single deterministic Python script (`run.py`), zero FDTD calls, implementing Q1–Q8 above exactly, producing a `results.json` and a printed report matching this document's own frozen numbers bit-for-bit (Q1-Q7 are already-verified desk arithmetic; Q4 and Q8 are the two genuinely load-bearing falsifiable claims, though both were also verified by this synthesis's own throwaway script before freezing — Phase 4 exists to make that reproduction independently auditable from a clean, committed script, not to discover a new answer). House gates: non-negativity/well-posedness checks on Firth's convergence, and a hard identity check that all 7 Table-1 numbers reproduce bit-exact from each source experiment's own `results.json`, not from any hand-typed literal (R4 discipline).

# PHASE 2 — CRITIQUE · VISION SCIENCE · Panel Iteration 65 · exp-088

Fresh context, no memory of any other seat's current-cycle output. Read in
full: LOGBOOK.md's RULED OUT (R1–R13) and T28's complete LIVE THREADS
history through Iteration 64/exp-087; PANEL.md; this cycle's
`phase1_proposal.md`; exp-087's `NOTES.md`; `experiments/083-.../
results.json::per_theta` (independently recomputed, not trusted from the
proposal's own table — see below).

## Steel-man

This is disciplined instrument work that has *already learned* this
sub-thread's own scars. It scores zero comparisons against a perceptual
threshold (`C_thr` never appears), correctly skips re-running NETD with an
explicit, reasoned justification (Idealization 9) rather than padding the
call count for appearance, and — unlike exp-087's own NOTES.md, which
called `frac_contrast` "localized Weber contrast" — never once reaches for
perceptually-loaded vocabulary; it says `frac_contrast` and stops. §4's
"desk-computable margins" table is exactly that: I independently
recomputed RMS, FLOOR, and all five margins directly from
`experiments/083-.../results.json::per_theta` (bit-exact to stated
precision) and from `experiments/087-.../results.json` — every cited
number reproduces. The retroactive floor-gate application to exp-087's own
filed classification, rather than only protecting future data, is the
right kind of intellectual honesty.

## Sharpest attack

Idealizations 9 and 10 — the NETD-is-not-eye disclaimer and the
"does-not-test-constraint-3" framing — appear exactly **once**, in §5, and
are never carried inline into §6, the section that actually restates the
P7-equivalent classification as this cycle's own headline finding three
separate times: Q1 ("Predicted corrected classification... under R13:
CONSISTENT (down from the filed ENERGY-DOMINANT)"), Q4 ("Both predicted to
classify 'C' (CONSISTENT)... NOT to reproduce or approach the 38.6°
spike"), and Q5 ("predicted to classify CONSISTENT overall... replacing
exp-087's own filed ENERGY-DOMINANT headline with a corrected,
gate-respecting CONSISTENT reading"). This is the identical shape this
seat has caught three consecutive times on this exact sub-thread
(T16/Iteration 53, R12/Iteration 63, exp-087/Iteration 64) — disclaimer
present in raw framing, absent from the prose restatement a future
LOGBOOK/PLAN.md citation will actually quote — with an explicit standing
tripwire from Iteration 64's own close: **a fourth instance fires
Checkpoint criterion 4 automatically.** Because §6 is committed,
pre-frozen predictive text (not informal discussion), this gap already
sits in the document that Phase 3 is instructed to carry forward, and
Phase 4 is instructed to reuse "verbatim, unmodified" — the highest-risk
possible vector for this exact defect to recur a fourth time with zero new
words written.

Secondarily (minor, not load-bearing): Q5's "the first fully
R13-compliant classification of T28's energy-interception channel" and the
`NODE-UNRESOLVABLE` outcome label both use language a careless reader
could mistake for a resolvability/visibility verdict about the *scene*
rather than about whether one internal ratio's denominator is measurably
above its own noise floor — worth a one-line gloss, not a blocking issue.

## Verdict

**support-with-changes.**

Mandatory fix, matching the standard this exact seat set at exp-087's own
Phase 2 (adopted there as Red Team mandatory fix 8, "carried inline, every
restatement" — not filed once and compressed later): Phase 3's synthesis
and the eventual NOTES.md must carry Idealization 9's NETD disclaimer and
Idealization 10's "does not test constraint 3 — only its energy-ledger
bookkeeping" framing verbatim, inline, at every restatement of the P7/Q1/
Q4/Q5 CONSISTENT / ENERGY-DOMINANT / NODE-UNRESOLVABLE classification
language, including this proposal's own §6 before it is treated as frozen.
Independently verified, not trusted: the §4 RMS/FLOOR arithmetic and all
five floor-gate margins reproduce exactly from raw `experiments/083-.../
results.json::per_theta` data (RMS=1.91744×10⁻³, FLOOR=1.91744×10⁻⁴;
margins 3.88×/0.39×/6.59×/7.49×/8.02× at θ=36.0°/38.6°/41.8°/38.4°/38.8°
respectively — the proposal's own "7.50×" at 38.4° is a display-rounding
artifact of dividing already-rounded 4-sig-fig operands, not an error;
full precision gives 7.4946×, same verdict, clears comfortably), and the
Q4 linear-interpolation figures (4.137×10⁻³/4.499×10⁻³ at 38.4°/38.8°,
7.9% high at the 38.6° check point) reproduce from `experiments/087-.../
results.json::frac_p_abs`. No numeric or arithmetic defect found anywhere
in this cycle's own table.

## Flip-parameter

If Phase 3's synthesis does not adopt the inline-disclaimer mandatory fix
above — i.e., if it carries §6's Q1/Q4/Q5 language forward into NOTES.md
without Idealization 9/10 riding inline at each restatement — my verdict
flips to **oppose**: that is the exact configuration that tallies as this
sub-thread's fourth disclaimer-erosion instance and fires Checkpoint
criterion 4 automatically, no further deliberation, per Iteration 64's own
standing rule.

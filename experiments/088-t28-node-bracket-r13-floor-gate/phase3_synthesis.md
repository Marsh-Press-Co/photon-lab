# PHASE 3 — SYNTHESIS · exp-088 · Panel Iteration 65

Director's synthesis, resolving Phase 2 into the ONE frozen configuration
and predictions committed to git before any Phase-4 code runs.

## Disposition of Red Team's 10-item fix docket

**All 10 items adopted. Zero overridden.** Red Team's Phase-2 audit
independently re-derived every load-bearing number in the proposal and
all five critiques from primitives before ruling — nothing here is a
restated critique, and nothing in the audit conflicts with the underlying
8-call frozen configuration or its cost. There is no basis to override any
item: every blocking item is either free (a text/scope change) or
near-zero marginal FDTD cost (items 7-8 reuse numbers already computed for
the PRIMARY metric).

1. **[adopted, blocking]** Idealizations 9 and 10 now carry inline,
   verbatim, at every restatement of classification language below
   (§Predictions). This is the single Checkpoint-4-relevant item in this
   docket — Iteration 64's own close pre-committed that a fourth instance
   of the disclaimer-erosion shape fires criterion 4 automatically, and
   Red Team confirmed this cycle's own first draft was, unfixed, that
   fourth instance. Adopting this item is what keeps the criterion from
   firing.
2. **[adopted, standing-practice]** This document (and the eventual
   `NOTES.md`) opens its own Predictions section with a **"Carried
   idealizations" banner** naming, by number, every corroborative-only
   disclaimer governing the classification language that follows — see
   below. Proposed, not yet a numbered standing rule (R14 would require
   its own dedicated adoption text per house convention; flagged for a
   future cycle to consider formalizing if the banner proves effective
   across a second T28 write-up).
3. **[adopted, blocking]** Q5 is rescoped explicitly to "at the 5 sampled
   angles only," with the θ≈40.2°/41.4° tighter floor-gate margins
   (1.31×-1.48×, vs. 7.49×-8.02× for the angles actually bracketed)
   disclosed alongside §4's table.
4. **[adopted, blocking]** A concrete forward-tripwire queue item is named
   in NOTES.md's own Next section: measure `ratio_k` by real FDTD at the
   three other node-adjacent established-grid angles (≈37.1°/37.2°, 40.2°,
   41.4°) before any future LOGBOOK/PLAN.md entry describes this channel
   as CONSISTENT in a channel-general (not merely 5-point-sampled) sense.
5. **[adopted, blocking]** One sentence added bounding the bracketing
   test's own claim: it rules out only a feature ≳0.4° wide, grounded
   explicitly in T28's own established ~2.84-2.95° periodicity as the
   physical basis for that bound. "Decisive" is struck from the mechanism
   narrative's own framing.
6. **[adopted, blocking]** New Idealization 13: `FLOOR`/`RMS` are specific
   to `graded_black_shell`/600nm and must be independently recomputed, not
   reused numerically, for any other absorber article or wavelength this
   gate is later applied to.
7. **[adopted, zero marginal cost]** Phase 4 will compute
   `mixed_length_scale_regime` → `netd_disposition` at θ=38.4°/38.8°
   (both configs), reusing the already-required `p_abs_w` values, and
   report alongside P7 in `results.json`.
8. **[adopted, zero marginal cost]** Phase 4 will extend the
   `ratio_abs_ext`-vs-T9(0.51) cross-check to the two new angles from the
   same `widths_by_cell` data already computed for the PRIMARY metric.
9. **[adopted, blocking]** NOTES.md states explicitly: `experiments/087-.../
   results.json`/`NOTES.md` remain the unedited historical record of what
   the frozen Iteration-64 pipeline computed. This cycle's own Q1
   "CONSISTENT" reading is a separate, disclosed, R13-corrected reading
   for forward citation purposes only — not a retroactive edit of
   exp-087's own filed record. Any future citation of "T28's
   energy-interception classification" must specify which of the two it
   means. Q5's "replacing... headline" language (Red Team's §6.1 finding)
   is rewritten to "supplies a separate, forward-citable reading
   alongside" — removing the ambiguous "replacing."
10. **[adopted, optional/minor]** One-line gloss added: `NODE-UNRESOLVABLE`
    /"R13-compliant" refers to denominator-resolvability of one internal
    ratio construction, not a scene-visibility or constraint-3 verdict.

## Frozen configuration (unchanged from phase1_proposal.md §8)

8 new FDTD calls: 2 configs (C40, G40) × 2 angles (38.4°, 38.8°) × 2 legs
(empty, article), STEPS=2800. No item in the fix docket adds or removes a
single FDTD call — every fix is a scope/text/disclosure change or reuses
numbers already computed for the PRIMARY metric (items 7-8).

## NOTES.md

Committed alongside this file, before any Phase-4 code runs — see
`NOTES.md` for the full frozen Hypothesis/Setup/Idealizations(1-13)/
Predictions text incorporating all 10 items above verbatim.

#### ELECTROMAGNETISM — verdict: **support-with-changes**

**Steel-man:**
The `kappa_window` footprint (x in [357,457), y in [260,300)) is a byte-for-byte reuse of exp-001's own established `BEHIND` slice (`experiments/001-flashlight-statement/run.py`: `slice(CX+R_CLK+15, CX+R_CLK+115)` = exactly [357,457) at CX=252, R_CLK=90) — genuinely apples-to-apples geometry with `beam_behind`, at the identical STEPS=3200 the 24/24-gated original figure was itself computed under, so a common settling bias should largely cancel in the ratio. The `edge=80` fix removes a real, previously undisclosed confound. R22's "no sign convention" claim is locally sound: `kappa` is built from `sc.phasors()`-reconstructed magnitude-squared — the same quadrature machinery underlying `beam_behind`'s own `env`, non-negative by construction on both sides — and the window's downstream-direction sign is inherited unmodified, not re-derived, so no fresh sign risk is introduced here.

**Sharpest attack:**
The five new near-field gap-fill points (x=352–356) sit closer to the PEC core/shell than anything the 24/24-gated suite has validated for settling at STEPS=3200. VALIDATION.md's stage-20 finding is explicit and directly on point: settling to true CW steady state is governed by material loss, not domain size, and a near-lossless region adjacent to a reflective structure can carry residual field error ~100x larger than a lossy-region check at the same step count — recommending any new amplitude-reconstruction technique "carry its OWN empirical noise-floor validation leg." This proposal inherits STEPS=3200 with zero settling-independence check for exactly these new points, in exactly the multiple-reflection/standing-wave-prone geometry that lesson warns about. Gates A/C/D are all self-consistency checks on one under-settled snapshot — none tests against a converged reference — so an unsettled bias could pass every gate and masquerade as the "one local reversal" the standoff-trend prediction treats as merely tolerable, when it's actually a settling artifact, not physics.

**Parameter change that would flip my verdict:** Add one settling-independence leg mirroring VALIDATION.md's own stage-20 precedent — rerun the x=352–356 gap-fill points at 2x STEPS (6400) and require `kappa_region` there to be stable within FLOOR_FRAC-scale tolerance across the two step counts before the standoff-trend monotonicity claim is trusted.

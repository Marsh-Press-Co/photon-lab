#### MATERIALS & METAMATERIALS — verdict: **CONFIRM-WITH-GAPS**

*(This cycle's own rotation-lead seat, self-reviewing with fresh eyes, not defending the earlier draft.)*

## Independent recomputation (from `results.json` raw fields)

All recomputed directly from `results.json`'s raw arrays via a standalone script, not by trusting `NOTES.md`'s prose:
`kappa_window` (exact), floor-gate RMS over 16 `i_region_empty` values (exact), reversal count (0, strictly increasing every step), window-span mean/ratio (exact), settling rel_change at x=352/x=356 (exact), κ(x=352)/κ(x=456) as % (matches NOTES prose).

No arithmetic, aggregation, or transcription error found anywhere between raw JSON, `run.py`'s console output, and `NOTES.md`'s prose. I also independently traced the `edge=80→40` provenance claim into the actual source files: `experiments/065-.../design_geometry.py:123` gives `TAPER=40`; `experiments/069-.../design_geometry.py:228-256` gives `R4_RATIO=2.0`, `R4_CPL={600:40}`, `R4_TAPER=round(TAPER*R4_RATIO)=80` — confirmed byte-for-byte. Likewise traced `GATEB_D_STANDOFF=100` back to `experiments/102-.../run.py:311-318` (`200*78/156=100`) — confirmed. I also read `lab/thermo_sidecar.py:124-170` directly and corroborated fix-7's disclosure. `run.py`'s floor-gate implementation is a verbatim match to `exp-102/run.py:461-469`'s `floor_gate()`, confirming fix 3's "not a departure" claim rather than restating it uncritically.

## Findings

**1. [LOAD-BEARING — MATERIALS-charter gap]** The Realizability Bound reasoning was silently dropped between Phase 1 and the final NOTES.md, leaving a dangling forward-reference nobody caught. My own `phase1_proposal.md` §6 contained the actual realizability argument: two specific, falsifiable-in-a-future-cycle physical expectations for a realizable substitute coating — (a) less smoothness (discrete grading layers would imprint periodic ripple), (b) a higher near-field floor (Kramers–Kronig causality + finite `r_out−r_in` bound how much extinction a real absorber packs into a given thickness). `NOTES.md` line 175 explicitly promises this content — "(Realizability Bound below)" — but no such section, nor any equivalent reasoning, appears anywhere later in the document (grep-verified: "layer", "Kramers", "realizab" appear zero times after line 175). The status *label* survived; the *reasoning my own charter is supposed to own* did not. None of the five blind Phase-2 critiques nor Red Team's audit flagged this — exactly the kind of self-scrutiny failure this program's own precedent warns a motivated rotation-lead is prone to, and it is squarely on me as this cycle's original author to have caught it before Phase-5.

**2. [NON-LOAD-BEARING — confirmed independent, no conflation]** The `edge=80→40` fix and the Realizability Bound reasoning are genuinely orthogonal, and stayed that way through the correction. `edge` governs the source's launch-aperture taper — an illumination-construction parameter with zero physical connection to the shell's own material grading. I checked the one place conflation could plausibly have crept in (Prediction 1's band-widening rationale) — `NOTES.md`'s text was correctly updated to cite `edge=40 vs. edge=24` instead of the stale `edge=80` framing. No conflation found.

**3. [NON-LOAD-BEARING — survives, arguably strengthens]** The clean, zero-reversal monotonic trend does not contradict my own realizability argument — it sharpens its falsifiability for a future cycle. This run only measured the idealized continuous-grading article, but the demonstrated smoothness (16/16 points strictly increasing, 20% settling margin cleared by 2–4 orders of magnitude) is the correct continuum-limit control for a future discrete-layer-coating comparison to difference against.

**4. [NON-LOAD-BEARING — new materials-science reading]** Re-anchoring the standoff sample points to the shell's own physical outer surface (`r_out=78`, not `R_CLK=90`, exp-001's *cloak* radius reused only because that file fixed one window across all three of its scenes): the sampled range runs from 1.10λ at x=352 to 6.30λ at x=456 from the coating's actual surface, over which κ rises ~14×. The shell's own radial grading thickness is `r_out−r_in=48` cells = 2.4λ. The healing length needed for κ to climb an order of magnitude (~5λ of standoff) is roughly *double* the grading thickness — suggestive that fill-in is edge-diffraction-dominated by the shell's overall transverse silhouette (2·r_out=7.8λ diameter), not by how deep the sigma grading runs radially. Predicts the grading thickness principally sets the *floor* of κ at the shell's own surface, while the transverse silhouette size sets how *fast* it heals downstream — two different physical knobs. If Next-item-5's "dense-standoff-trend fit" is picked up, it should be parameterized against `r_out` (silhouette), not `r_out−r_in` (grading depth).

**5. [NON-LOAD-BEARING — unverifiable in this review]** The "trust suite confirmed green (41/41) before and after" claim has no corroborating artifact in this experiment's directory. This cycle made zero `lab/` diff so VALIDATION.md's own re-run trigger doesn't strictly require it, but the specific "41/41" figure is asserted, not shown, here.

## Argued next change

Before this cycle is treated as closed, restore an actual Realizability Bound section to `NOTES.md` — not just the status label — carrying forward (and sharpening with this cycle's own data) the two falsifiable expectations from `phase1_proposal.md` §6, explicitly connected to what was just measured: the demonstrated zero-reversal smoothness as the continuum-limit control a future discretely-layered-coating comparison needs, and the `r_out` (silhouette) vs. `r_out−r_in` (grading depth) distinction from finding 4 as a stated hypothesis for the future dense-standoff-trend fit already queued in Next-item-5 — otherwise the realizability reasoning this program relies on MATERIALS to own has, in practice, quietly evaporated from the record.

#### THERMODYNAMICS — verdict: **support-with-changes**

**Steel-man:**
The "not touched" claim checks out against the actual code. `thermo_sidecar.absorbed_power_established_ratio` derives `p_abs_w` from `sigma_ext_cells` (sourced from exp-059's Mie/qext-theory extinction-width calculation) times an externally assumed `i_incident_w_cm2`, and `ratio_abs_ext` — none of which reads from Gate B's own FDTD source construction, amplitude, or aperture `edge` parameter. The graded_black_shell absorbed-power pipeline that produced 699.27x (exp-057, pinned from exp-043/exp-059, "NOT re-derived") never touches `beam_behind` or kappa at all. Since this cycle invokes zero thermo_sidecar calls and persists no sidecar field, R21's own trigger — a persisted field's headline finding going unnarrated — literally cannot fire; there is nothing to narrate. Structurally this is the same posture exp-102 correctly took.

**Sharpest attack:**
"Not touched" is asserted by precedent-matching exp-102, not demonstrated. Section 5 never cites the actual independence chain (p_abs_w ← exp-059 Mie-theory `sigma_ext_cells` × assumed irradiance, decoupled from Gate B's FDTD source) that makes the claim true — it just declares non-engagement. Since this cycle deliberately reruns the *exact* flagship geometry (r_in=30/r_out=78, pec r=30) that backs the 699.27x citation, at a materially different source aperture (edge=80 vs 24), a reader cannot tell from this text whether independence was checked or merely assumed. That gap invites a future citation error: someone treating this cycle's aperture-matched `kappa_window` as the figure now underwriting the existing thermal-UNDETECTABLE margin, when the two pipelines never shared inputs. One disclosed sentence closes it; its absence is a real, if currently low-consequence, provenance risk on a T28-adjacent channel this seat is chartered to police.

**Parameter change that would flip my verdict:** Add one sentence to Section 5 naming the dependency chain explicitly (p_abs_w ← sigma_ext_cells [exp-059 Mie/qext theory] × assumed i_incident_w_cm2, independent of Gate B's own FDTD source `edge`) — turning "not touched" from an asserted precedent-match into a verified-independent claim. With that line added, this seat supports outright.

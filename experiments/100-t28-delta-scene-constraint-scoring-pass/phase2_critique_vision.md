# Phase 2 Critique — VISION SCIENCE

## Steel-man

Leg A's zero-FDTD instrument-floor discipline is exactly right, and correctly
reuses this seat's own established practice: it bounds any PASS/FAIL verdict
against T16's measured 7.80×10⁻⁴ combined domain+quadrature swing and T24's
measured 0.002–0.007 boundary systematic (both verified against LOGBOOK.md,
correctly cited) rather than reporting a clean physical reading — the largest
filed `delta_scene` value (2.778×10⁻³) sits inside that floor, and the
proposal says so unprompted. Scoping the claim strictly to "36°–43°, this
specific already-measured angle set" (Idealizations 64/66) avoids the
extrapolation error Red Team caught in this program's own Iteration-1
witness-scenario table. And §3's refusal to let a Leg-A sub-threshold reading
stand in for a constraint-1/2 verdict ("a Weber-contrast-scale ripple and a
beam-extinction fraction are different physical quantities") is the correct
perceptual/physical distinction — exactly the discipline this seat exists to
enforce.

## Sharpest attack

Two compounding problems, both in §Tier-2-Leg-A. **(1) Construct validity**:
`C_thr(L)` is T2's STATIC, steady-adaptation, extended-uniform-patch
threshold — Iteration 1's own idealization (ii) pins it as scoring "area
contrast, not edge-profile detectability." `delta_scene(θ)` is not a static
patch contrast; its content is a ~2.9474°-period oscillation revealed AS θ
sweeps — exactly the transient/modulation regime T3 exists to score, and
T3's joint constraint-3/4 verdict is still this program's longest-standing
unbuilt instrument (LOGBOOK, current). This seat's own Iteration-24 Phase-5
review already ruled this composition invalid for a different transient
quantity ("C_thr(L) is a static-target threshold applied to a physically
transient event, T3 still unbuilt") — uncited, unaddressed here. **(2)
Citation**: the "pinned witness-scenario scotopic anchors (L*≈5×10⁻⁶–4×10⁻⁵,
moonless-rural≈1.7×10⁻⁴)" are Iteration-1's raw Phase-1 draft numbers,
overturned by Red Team's own Phase-5 audit and replaced at Phase-3 synthesis
by L*_field∈[1.7×10⁻⁴,1.2×10⁻³] cd/m² — cited here as "T2's own committed
table verbatim."

## Verdict

**Support-with-changes.**

## Flip parameter

Replace the stale anchors with the corrected Phase-3 band
(L*_lab∈[5.3×10⁻⁶,7.5×10⁻⁵], L*_field∈[1.7×10⁻⁴,1.2×10⁻³] cd/m²) and add an
explicit caveat that Leg A's PASS/FAIL reading is a static-contrast bound
only, provisional pending T3's still-unbuilt joint transient instrument —
not a completed Tier-W/Tier-A scoring of a swept angular fringe — and I move
to full support. If Leg A's "complete both tiers" language ships unchanged,
carrying the superseded numbers as "verbatim T2," I move to oppose.

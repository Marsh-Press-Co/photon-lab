# Phase 2 Critique — MATERIALS & METAMATERIALS

## Steel-man (≤150 words)

The proposal's Tier-1 item 2 gets the taxonomy right and cites sources
precisely: "cpl-is-inert" (`L_GEOMETRIC_M` invariant to 1e-12, R3/R4/R5,
confirmed) is correctly separated from `delta_scene`'s unresolved
realizability status, and the exact provenance — Iteration 59's rule,
Iteration 60's explicit non-reinstatement ("genuine ambiguity remains
between two opposite-realizability readings") — is cited and verified
against LOGBOOK.md verbatim. Critically, the memo does not overclaim: it
states plainly that item 1's partition, not item 2 itself, is "the first
work... actually aimed at" the open question, avoiding the R4-class
hand-waved-resolution failure this program has been burned by five times
in exp-099 alone. Scoping it zero-FDTD and citable is proportionate to a
documentation task. This is disciplined bookkeeping, not physics posing
as resolution.

## Sharpest attack (≤150 words)

Tier-1 item 2 is framed as a MATERIALS disposition, but by its own text
(§Tier 1 item 2) it "formaliz[es] the split already drafted" — producing
zero new determination. That's a category error, not modesty: my
charter's realizability question (published/plausible/unobtainium)
applies to a PROPOSED structure achieving new optical behavior.
`delta_scene` offers no such candidate. Either it's PAD-inherited
(exp-076's proven lossless-vacuum proof — no material implicated at
all), or it's genuine diffraction off the ALREADY-BUILT, published-tier
`graded_black_shell` — its 2.9474° period sits within 3.7% of
`P_edge_A`, which PHOTONICS traced at Iteration 60 to the EMPTY-scene
exp-069 run (zero materials calls). Neither pole needs a realizability
tier: one has no material, the other needs no new one. The memo, as
scoped, cannot produce anything but a fourth restatement of an ambiguity
already stated at Iterations 59, 60, and 76 — inert regardless of item
1's outcome.

## Verdict

**support-with-changes.**

## Parameter that would flip the verdict

Rescope item 2 from a flat restatement into a pre-registered conditional:
MATERIALS states, *before* item 1 runs, what realizability tier follows
from EACH of item 1's two possible outcomes (majority-PAD residual →
zero-content, no tier assigned; genuine article-coupled residual → tier
assigned against the residual's actual magnitude/period, e.g. whether it
implies anything beyond ordinary diffraction off the already-published
`graded_black_shell` geometry). That would make item 2 a real,
falsifiable disposition rather than a label, and would move my verdict to
unqualified support.

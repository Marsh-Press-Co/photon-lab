# PHASE 2 — CRITIQUE · VISION SCIENCE · Panel Iteration 64 · exp-087

## Steel-man

This is a clean, correctly-scoped instrument cycle. It discharges a
genuinely overdue tripwire (fifth-deferral, per Iteration-63's own
pre-announcement) with a purpose-built measurement rather than another
desk deferral. Crucially for this seat, it never scores anything against
a perceptual threshold: `C_thr` does not appear anywhere in §4's
predictions, and `frac_p_abs`/`frac_contrast`/`ratio_k` are all
contrast-to-contrast or power-to-power ratios — commensurable by
construction, correctly learning R9's lesson (the T16 `amp_ratio`-vs-
`C_thr` unit mismatch) rather than repeating it. NETD carries its
standing instrument-not-eye disclaimer (idealization 9), matching
`thermo_sidecar.py`'s own baked-in disclaimer text verbatim. `git log`
confirms only `phase1_proposal.md` is committed — zero Phase-4 code
exists yet, satisfying pre-registration discipline. P5's classification
bands are genuinely falsifiable numeric outcomes, not a rhetorical
placeholder.

## Sharpest attack

Idealization 10 names constraint 3 affirmatively — "bears... on
constraint-3's energy-ledger bookkeeping" — the one constraint NOT in
its own exclusion list ("does not test constraints 1/2/4"). Nothing in
§4 currently violates this: P6's NETD "UNDETECTABLE" stays instrument-
scoped, idealization 9's disclaimer is intact, and PANEL.md's own Metrics
table lists the energy ledger as its own unconstraint-numbered row, not
literally part of constraint 3. But this exact phrase is the linguistic
seam that produced two prior VISION-caught scope-erosion incidents on
this sub-thread: exp-076/Iteration-53's T16 mislabeling and
exp-086/Iteration-63's Learned-section widening of a scoped finding into
an unqualified claim. Nothing stops Phase 3/5 prose from paraphrasing
"bears on constraint-3's bookkeeping" into "this cycle addresses/bears on
constraint 3" without the disclaimer riding along — the defect isn't
here yet, but the seam that produced it twice before is.

## Verdict

**support-with-changes.**

Mandatory fix: NOTES.md and every Phase-3/5 restatement of this cycle's
scope must carry idealization 9's NETD disclaimer and idealization 10's
"does not test constraint 3" framing (constraint 3 is fed only a
bookkeeping/energy-ledger number, never a Tier-W/Tier-A verdict)
verbatim and unabbreviated wherever P6 or the constraint-3 language is
restated — not merely stated once in the frozen Phase-1 text and then
compressed later, matching the R9/T16 and R12/Learned-section precedent
this exact seat has caught twice on this sub-thread.

## Flip-parameter

If §4-P6 or §1 anywhere quoted "UNDETECTABLE" as a bare figure — without
idealization 9's instrument-not-eye qualifier riding inline at the point
of use, not just filed once in §5 — my verdict flips to **oppose**: that
is the exact shape (an instrument threshold quietly standing in for a
human-perceptual one) constraint 3 exists to guard against.

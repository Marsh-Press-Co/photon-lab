# VISION SCIENCE — Phase 2 Critique, Iteration 29

**Steel-man:** §4's declination is correct as stated, and disciplined. This object sits at |C|≈0.68–0.76 — nowhere near T2's frozen C_thr(L)≈0.005·max[1,(L/3)^−p] ladder, so PASS/MARGINAL/FAIL language genuinely doesn't apply, and the proposal is right not to borrow vision-science authority it hasn't earned. It correctly separates a realizability/geometry question (does a fixed-thickness coating shallow or deepen with scale?) from a threshold question, and doesn't smuggle a Tier-W/Tier-A claim in through the back door. That boundary discipline — refusing to score against a ladder that doesn't bind — is exactly what my charter asks for, applied honestly against my own instrument rather than for it.

**Sharpest attack:** The T14-verdict narrative (P-1/P-2/P-3) stakes a scientific conclusion on C-deltas as small as 0.0010 (P-2, r=312) using the plain N9 fallback angle set, with no re-verified angular-quadrature convergence at either new geometry for this new object. T16 measured this program's own N9-quadrature-plus-domain uncertainty budget at r=156 to be 7.80×10⁻⁴ — comparable to, not negligible against, P-2's 0.0010 margin — and found the swing does NOT shrink monotonically with r_out (worse at r=78-native than r=156). r=312's own quadrature convergence has never been checked at all. So P-2's REFUTED/CONFIRMED call, if r=312 runs, risks reading angular-sampling noise as the T14 mechanism. P-1's 0.0046 r=156 margin is ~6× the known r=156 budget and is comparably safer, but the proposal explicitly (§6, idealization 2) allows the r=312 leg to be dropped or, if kept, its number cited unguarded — the least-characterized reading is the one most likely to anchor the headline finding.

**Verdict:** support-with-changes

**Parameter change that would flip verdict:** Either (a) widen P-2's REFUTED/CONFIRMED bands at r=312 to ≥2× T16's measured r=156 budget (~0.0016) so the call isn't scored inside known-comparable instrument noise, or (b) run one cheap per-angle ±40° empty-scene floor spot-check at r=312 (already precedented, near-zero marginal cost per exp-030's own timings) before P-2 is trusted as CONFIRMED/REFUTED rather than PLAUSIBLE-but-uncharacterized.

## VISION SCIENCE — Phase 2 Critique, Panel Iteration 44 (exp-067 candidate)

**Charter-scope check (as instructed):** R_contact itself is not my object. `thermo_sidecar.py`'s NETD machinery explicitly disclaims itself as an instrument/detector threshold that must **NOT** bear on constraint-3/4's human-eye verdict (lines 402–405, 519–522, 541–568), and the proposal's primary item never touches contrast, luminance, or adaptation. I have no standing to attack the R_contact mechanism/gates. My charter's only real purchase is the **Secondary scope** paragraph, which reaches into Block ARTICLE — the *only* construction in this program's history to produce a scored constraint-3 Weber-contrast number (P-VIS42-6/7, LOGBOOK 13626–13628), currently sitting **retracted** and unresettled after two straight cycles.

**1. Steel-man (≤150 words).** Desk arithmetic (R_contact, series-resistance algebra, stage-25 gates) and FDTD compute-minutes are genuinely different resource classes — nothing about writing `bonded_substrate_conduction_correction()` consumes GPU/CPU budget the Block-ARTICLE leg needs. R_contact is locked and unconditional regardless of what else happens this cycle, so proposing to spend *leftover* cycle capacity on the highest-ranked FDTD item (Red Team's own Iteration-43 rank #2, and the only queued item that resettles my own charter's still-retracted P-VIS42-6/7 numbers) is more efficient than leaving that capacity idle. It also matches PLAN.md's own Iteration-44 lock language verbatim: "MATERIALS' own Phase-5 review recommends folding both into one Iteration-44 cycle if scope allows" — this isn't MATERIALS freelancing, it's executing a standing recommendation.

**2. Sharpest attack (≤150 words).** "If scope allows" has a two-cycle empirical answer already on the record, and it's no. Exp-065 (Iteration 42, VISION lead, 144 FDTD calls, full five-phase cycle) and exp-066 (Iteration 43, PHOTONICS lead, 39 calls, full five-phase cycle) were each devoted **entirely** to T27 FDTD work and *both* still left Block ARTICLE's article-present legs, the four interior FALLBACK_ANGLES, and Block MINI open (LOGBOOK 13625–13631, 13504–13507). This proposal asks a single cycle to do R_contact's real desk build (new sidecar function, new `r_contact_provenance` guard type, a four-gate stage-25 suite, a self-caught second anchor) as PRIMARY, then fit inside the *same* cycle what two dedicated cycles already failed to finish — with **zero committed FDTD call/time budget** for it, unlike every prior FDTD Phase-1 proposal in this record (exp-065 pre-committed 119→144 calls/17.2→21.3 min; exp-066 pre-committed and delivered 39 calls/3.7 min). "Secondary, if scope allows" is very likely a fourth quiet deferral of exactly the item my charter has standing on, dressed as inclusion rather than honestly re-ranked as deferred.

**3. Verdict: support-with-changes.** R_contact as primary/locked stands unmodified — not my charter's fight. The Secondary-scope paragraph should not enter Phase 3 synthesis as currently written.

**4. Parameter change that would flip me to full support:** Replace "recommends folding in... AFTER R_contact's desk work completes" with a pre-committed, capped FDTD budget (mirroring exp-065/066's own disclosure convention — an explicit call-count/minute ceiling) that triggers Block ARTICLE's article-present legs *only*, dropping FALLBACK_ANGLES/Block MINI from this cycle's scope entirely, and states up front that a miss defaults to an honest disclosed fourth-consecutive-deferral line in the NOTES.md rather than silent absorption into "secondary, if scope allows."

**Files referenced:** `/home/user/photon-lab/PANEL.md`, `/home/user/photon-lab/PLAN.md` (lines 2337–2378), `/home/user/photon-lab/LOGBOOK.md` (Iteration 42 exp-065: 13365–13554; Iteration 43 exp-066: 13555–13694), `/home/user/photon-lab/lab/thermo_sidecar.py` (NETD disclaimer, lines 402–405/519–522/541–568).
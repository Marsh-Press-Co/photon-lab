# PHASE 2 — CRITIQUE · THERMODYNAMICS · Panel Iteration 84

*Reviewing candidate exp-107, "The Properly-Powered R5 Census, a
Ground-Truth Recovery Gate, and Three Zero/Low-Marginal-Cost
`kappa_window` Closeouts" (Lead: VISION SCIENCE). Blind to all other
seats' current-cycle critiques.*

## Steel-man (≤150 words)

Item 3 closes genuine, standing debt this seat's own instrument carries:
the fixed-abs family has never had a thermal row at ANY r, and exp-106
explicitly declined to invoke the sidecar this cycle, leaving
THERMODYNAMICS' own charter question — does the fixed-abs family's
different, non-rescaled `sigma_max` change the absorbed-power budget
enough to matter thermally? — genuinely open even though the ledger's
`p_abs_frac_diff` already answered the narrower box-conservation
question. Correctly scoped as zero-marginal-FDTD-cost: `sigma_ext`/
`sigma_abs` for both families at r=156/312 already sit in exp-106's
`results.json`, no new `Sim.run()` call is implied, and the item is
labeled a post-run analytic add-on, honoring the sidecar's own
expressibility contract. Anchoring on "matching every prior T28-bridge
cycle" is also a reasonable prior — every established-ratio article
tested since exp-043 has cleared two-plus orders of magnitude below
NETD — so expecting a fourth cheap confirmation is not laziness.

## Sharpest attack (≤150 words)

Item 3's predicted band — "UNDETECTABLE at all 4 (family,r) cells, ≥100×
margin, matching every prior T28-bridge cycle" — is not what is already
computable from exp-106's own persisted fields. My own exp-106 Phase-5
review derived, from raw `sigma_ext`/`sigma_abs`, that `p_abs_w ∝
σ_ext·σ_abs` (not `σ_abs` alone — `iso_xsec_sq`'s area scales as
`σ_ext²`), and applied that ratio to exp-105's own established margins
(699.27×/349.80×/175.06× at r=78/156/312, self-similar): `margin_
fixedabs(156)≈267×`, but `margin_fixedabs(312)≈120×`. That is a real,
already-derivable number, not a guess — and 120× is a weak "≥100×," only
20% above the stated floor, nothing like the other three cells' 267×–700×
headroom. The proposal's own FALSIFY band (margin <10×) cannot register
this at all: a genuine physical effect eroding the true margin by another
full order of magnitude would still pass cleanly. (fixedabs, r=312) is
the fragile cell here, mis-stated as routine.

## Verdict

**Support-with-changes.**

## Single parameter change that would flip my verdict to support

Before Phase 3 freezes, replace the blanket "≥100× margin at all 4
cells" prediction with per-cell numeric bands that state the
(fixedabs, r=312) cell's own already-derivable expected value (≈120×,
not ≈700×) explicitly, and add an explicit R21 commitment — this is
literally THERMODYNAMICS' NETD channel, already at two non-firing
founding instances (exp-099, exp-100) with a live three-strike
auto-fire clause — to narrate whichever number Item 3 actually computes
inline in NOTES.md's Result section, not merely persist it to
`results.json`. Both fixes are zero-marginal-cost text changes, not new
computation.

# PHASE 2 — CRITIQUE (PHOTONICS) · Panel Iteration 84 (candidate exp-107)

## Steel-man (≤150 words)

This is the correctly-disciplined execution of R15's own addendum, applied
to this specific sub-thread for the first time rather than merely cited: a
third, *differently*-ratioed resolution family (R5_RATIO=2.5, not another
1.5×/2.0× multiple) gated by a pre-registered ground-truth sign-AND-
amplitude recovery check (G0) at a far-from-null anchor, computed from
already-filed data before any FDTD call — exactly the "does not default to
the finer grid" and "must reproduce the ALREADY-KNOWN-CORRECT sign... before
that point is trusted" discipline R15 mandates. The single-provenance n=31
grid is a genuine angular-sampling improvement over R3/R4's own pooled,
differently-scoped patchwork. The exhaustive, mutually-exclusive three-way
outcome table, with a pre-committed formal-retirement branch, correctly
forecloses an eighth silent deferral without smuggling in a bias toward
"convergence found." The ≥1.4° anchor buffer is well-matched (≈half-period)
to the established 2.84–2.95° fringe.

## Sharpest attack (≤150 words)

The proposal's central safety claim — "`delta_scene`'s established
magnitude, R9-corrected, is ≈0.08–0.12× ... C_thr=0.005 (LOGBOOK T16)" —
cites the wrong statistic for what it's used to justify. T16/R9's
0.08–0.12× figures are `amp_ratio`-normalized, fitted-sinusoid amplitudes
for the `PAIR_PAD`/`PAIR_ABSORB40` differences (exp-076/077) — a smoothed,
single-tone-fit quantity. The directly on-point precedent, already sitting
in this cycle's own cited source pool — exp-100's Tier-2 Leg A, scoring the
*same* pooled 36°–43° table this census extends — computed the raw
peak `|delta_scene(θ)|=3.1495×10⁻³` at θ=39.2° against the identical
`C_thr_lab=0.005` and got **63% of the bar**, not 8–12%: a ~5–6× discrepancy
in "how close to threshold" between two statistics of the nominally same
signal, unreconciled anywhere in this text. The G0 gate's own `[0.5,2.0]`
amplitude-ratio band and the 1.4° buffer are likewise asserted with no
citation to any on-file far-from-null cross-resolution amplitude figure
(R17's own discipline) — the whole "sub-threshold whichever family is
right" framing leans on the more comfortable, less relevant number.

## Verdict

**Support-with-changes.**

## Single parameter change that would flip my verdict

Replace the §1 magnitude claim with exp-100's own directly-relevant figure
(peak `|delta_scene|` reads 63% of `C_thr_lab`, not 8–12%) and require Phase
3 to independently justify (or compute from already-filed data, zero new
FDTD) the G0 amplitude-ratio band `[0.5,2.0]` against the largest on-file
far-from-null cross-resolution amplitude swing before freezing — absent
that, I'd move to oppose, since the census's entire "no verdict is at risk
either way" premise rests on the unreconciled, more favorable number.
